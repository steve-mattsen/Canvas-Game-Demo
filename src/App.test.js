import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders game window', () => {
//   let result = render(<App />);
//   const canvas = result.container.querySelector('#game_window')
//   expect(canvas).toBeInTheDocument();
// });
